define(["jquery"], function($) {
    // libs
    require(['bootstrap.min', 'd3.min']);

    // app
    require(['app/goodies']);
    require(['app/torus-array']);
    require(['app/rle-parser']);
    require(['app/life']);
});
